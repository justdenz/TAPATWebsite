const express = require('express')
const router = express.Router()
const Initiative = require('../models/InitiativeModel')

// router.get("/", async (req, res) => {
//     res.send(await Initiative.getInitiative())
// })
module.exports = router